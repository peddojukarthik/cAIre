fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
pgvector==0.3.4
pydantic==2.9.2
pydantic-settings==2.5.2

# Auth + JWT verification (Supabase issues standard JWTs)
python-jose[cryptography]==3.3.0

# Google AI Studio (Gemini) -- free-tier API key, no GCP project required
google-generativeai==0.8.3

# App-level document signing (replaces Cloud KMS)
cryptography==43.0.1

# Supabase Storage / Auth admin helpers (optional, for storage bucket access)
supabase==2.7.4

python-multipart==0.0.12
